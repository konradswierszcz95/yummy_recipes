spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yummy_recipes
    username: konrad
    password: konrad
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true
server:
  servlet:
    context-path: /yummier